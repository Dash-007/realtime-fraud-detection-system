# Test Hugging Face setup locally
version: '3.8'

services:
  fraud-detection:
    build:
      context: .
      dockerfile: Dockerfile.hf
    ports:
      - "7860:7860"
    environment:
      - SPACE_ID=test-local
    volumes:
      - ./models:/app/models:ro